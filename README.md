# Dotfiles : Spark
Spark のクラスタを運用するための設定ファイルです。

# Install Spark to local VM as master
```
make install
```

# Create worker VMs and install spark
```
make worksers
```

# クラスタの実行
```
make start-cluster
```
